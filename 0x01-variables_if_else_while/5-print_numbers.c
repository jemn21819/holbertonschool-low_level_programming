#include <stdio.h>

/**
 * main - print single digit with base 10
 *
 * Return:  Always 0 (success)
 */

int main(void)
{
	int x;

	for (x = 0; x <= 9; x++)
	{
		printf("%d", x);
	}
	printf("\n");

	return (0);
}
