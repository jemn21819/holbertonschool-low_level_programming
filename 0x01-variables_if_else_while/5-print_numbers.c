#include <stdio.h>

/**
 * main - Pritn alphabet in lowcase
 *
 * Return:  Always 0 (success)
 */

int main(void)
{
	int x;

	for (x = o; x <= 9; x++)
	{
		printf("%d", x);
	}
	printf("\n");

	return (0);
}
